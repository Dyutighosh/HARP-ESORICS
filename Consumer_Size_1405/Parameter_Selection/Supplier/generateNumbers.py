import numpy as np

# Assuming global_min and global_max are already defined
global_min = 210.0 #25039.10669694587  # Replace with your actual global minimum value
global_max = 966.9879817143079 #43535.01033272372  # Replace with your actual global maximum value

# Generate 1000 numbers between global_min and global_max
generated_numbers = np.random.uniform(global_min, global_max, 1000)

# Convert to a list and sort (although np.linspace already returns a sorted array)
sorted_numbers = sorted(generated_numbers.tolist())

# Print the sorted list
print(sorted_numbers)

generated_numbers =  np.random.uniform(global_min, global_max, 1000)

# Convert to a list and sort (although np.linspace already returns a sorted array)
sorted_numbers = sorted(generated_numbers.tolist())

# Print the sorted list
print(sorted_numbers)


generated_numbers =  np.random.uniform(global_min, global_max, 1000)

# Convert to a list and sort (although np.linspace already returns a sorted array)
sorted_numbers = sorted(generated_numbers.tolist())

# Print the sorted list
print(sorted_numbers)


generated_numbers =  np.random.uniform(global_min, global_max, 1000)

# Convert to a list and sort (although np.linspace already returns a sorted array)
sorted_numbers = sorted(generated_numbers.tolist())

# Print the sorted list
print(sorted_numbers)


# Global Min: 29505.0
# Global Max: 93544.39454597165